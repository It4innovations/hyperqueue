[workspace]
members = [
    "crates/hyperqueue",
    "crates/tako",
    "crates/pyhq"
]
default-members = [
    "crates/hyperqueue",
    "crates/tako"
]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
# TODO: disable panic = "abort" for pyhq build
panic = "abort"
strip = "debuginfo"
